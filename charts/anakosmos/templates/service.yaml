apiVersion: v1
kind: Service
metadata:
  name: {{ include "anakosmos.fullname" . }}
  labels:
    {{- include "anakosmos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anakosmos.selectorLabels" . | nindent 4 }}
